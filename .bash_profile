#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

#[ "$(tty)" = "/dev/tty1" ] && ! pgrep -x i3 >/dev/null && exec startx

export EDITOR="vim"
export TERMINAL="st"
export BROWSER="firefox"
export READER="zathura"



